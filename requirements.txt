# CLIPGCN runtime dependencies.
#
# These pins match the current clipgcn environment closely enough for
# train/test plus webcam_realtime.py. For CUDA Docker images, install with:
#   pip install -r requirements.txt
#
# External repositories/checkpoints are still required at the paths used by the
# scripts, for example /workspace/X3D, /workspace/CTR-GCN, and /workspace/yolov5.

--extra-index-url https://download.pytorch.org/whl/cu118

# Core tensor/runtime stack.
torch==2.5.1+cu118
torchvision==0.20.1+cu118
numpy==2.2.6
PyYAML==6.0.3
tqdm==4.67.3

# CLIP text encoder used by model.py.
ftfy==6.3.1
regex==2026.2.28
clip @ git+https://github.com/openai/CLIP.git

# Realtime webcam / raw end-to-end inference.
opencv-python==4.13.0.92

# RTMPose COCO-17 skeletons: the estimator the COCO-17 CTR-GCN backbone was
# trained on, and therefore the default pose source for those checkpoints.
# Swap onnxruntime-gpu for onnxruntime on machines without CUDA (~7x slower).
rtmlib==0.0.15
onnxruntime-gpu==1.19.2

# Only needed for the older 25-joint checkpoints, which used a MediaPipe->NTU
# mapping for their skeleton stream.
mediapipe==0.10.35

# YOLOv5 local torch.hub dependencies used by test_raw_end_to_end.py and
# webcam_realtime.py. Keep these here so a laptop Docker image can run the
# object branch after mounting/cloning /workspace/yolov5.
gitpython>=3.1.30
matplotlib==3.10.8
pandas==2.3.3
pillow==12.0.0
psutil>=5.9.0
requests==2.34.2
scipy==1.15.3
seaborn==0.13.2
thop>=0.1.1
ultralytics>=8.4.47
packaging>=23.0
setuptools>=70.0.0
urllib3>=2.6.0
